# 🌱 Blooming Garden 自動プレイヤー 🌸

このプロジェクトは、[Blooming Garden](https://in0ho1no.github.io/2025-adhoc-blooming-garden/)（2048風ゲーム）を自動でプレイするPythonスクリプトです。

## 📋 必要要件

- Windows 10/11
- Python 3.12以上
- uv（Python パッケージマネージャー）推奨

## 🚀 セットアップ

### 方法1: 自動セットアップ（バッチファイル版・推奨）

1. `setup.bat` をダブルクリック
2. 画面の指示に従う（英語表記）
3. 完了！

> **推奨理由**: バッチファイル版は実行ポリシーの設定が不要で、確実に動作します。
> **注意**: 文字化け防止のため、メッセージは英語で表示されます。

### 方法2: 自動セットアップ（PowerShell版）

1. `setup.ps1` を右クリック → **「PowerShellで実行」**
2. 画面の指示に従う
3. 完了！

> **注意**: 初回実行時に「スクリプトの実行が無効」エラーが出る場合は、以下を実行：
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```
> 
> **PowerShellウィンドウが一瞬で閉じる場合**:
> 1. プロジェクトフォルダで **Shift + 右クリック**
> 2. 「PowerShellウィンドウをここで開く」を選択
> 3. `.\setup.ps1` を実行

### 方法3: 手動セットアップ

PowerShellを開いて：

#### uvを使用する場合（推奨）

```powershell
# uvのインストール（まだインストールしていない場合）
irm https://astral.sh/uv/install.ps1 | iex

# PowerShellを再起動

# 依存関係のインストール
uv sync

# Playwrightのブラウザをインストール
uv run playwright install chromium
```

#### pipを使用する場合

```powershell
# 仮想環境の作成
python -m venv .venv

# 仮想環境の有効化
.\.venv\Scripts\Activate.ps1

# 依存関係のインストール
pip install playwright

# Playwrightのブラウザをインストール
playwright install chromium
```

## 🎮 使い方

### 簡単実行（推奨）

#### バッチファイル版（確実）
`run_autoplay.bat` をダブルクリック

#### PowerShell版
`run_autoplay.ps1` を右クリック → **「PowerShellで実行」**

モード選択画面が表示されます：
- 1: 基本版（シンプルな戦略）
- 2: 高度版（グリッド分析戦略）

### 手動実行

PowerShellまたはコマンドプロンプトを開いて：

#### 基本版の実行（シンプルな戦略）

```powershell
uv run python src/autoplay.py
```

または仮想環境を有効化している場合：

```powershell
python src\autoplay.py
```

**特徴:**
- シンプルな戦略（左→下を中心に移動）
- 軽量で高速
- 初心者向け

#### 高度版の実行（グリッド分析戦略）

```powershell
uv run python src/autoplay_advanced.py
```

または仮想環境を有効化している場合：

```powershell
python src\autoplay_advanced.py
```

**特徴:**
- グリッドの状態を読み取る
- 最大タイルを左下隅に維持する戦略
- 行き詰まり検出機能
- より高いスコアを目指す

## 📊 スクリプトの動作

### 基本的な流れ

1. Playwrightでブラウザを起動
2. ゲームページにアクセス
3. キーボード操作（WASD）でゲームをプレイ
4. ゲーム状態を監視（スコア、最大タイル）
5. 2048到達またはゲームオーバーまでプレイ
6. 結果を表示

### 出力例

```
🌱 Blooming Garden 自動プレイヤー 🌸
==================================================
URL: https://in0ho1no.github.io/2025-adhoc-blooming-garden/
戦略: 左下に大きなタイルを集める
==================================================

🎮 ゲームを開始します: https://in0ho1no.github.io/2025-adhoc-blooming-garden/
🌱 ゲームスタート！
🌸 新しいタイルに到達: 4 (スコア: 4)
🌸 新しいタイルに到達: 8 (スコア: 12)
🌸 新しいタイルに到達: 16 (スコア: 28)
...
📈 進捗: 100手目 (最大タイル: 128, スコア: 1580)
...
🎊 おめでとうございます！2048に到達しました！
📊 最終スコア: 24536
🎯 移動回数: 432

✅ 自動プレイを終了しました
```

## 🎯 戦略の説明

### 基本版（autoplay.py）

**戦略:**
- 左下に大きなタイルを集める
- 左（a）→下（s）を中心に繰り返す
- 10手に1回は上（w）を使用
- 30手に1回は右（d）を使用（行き詰まり回避）

**パターン:** `['a', 's', 'a', 's', 'a', 's', 's']`

### 高度版（autoplay_advanced.py）

**戦略:**
1. グリッドの状態を常に監視
2. 最大タイルの位置を追跡
3. 最大タイルを左下隅（row=3, col=0）に維持
4. 2番目に大きいタイルを隣に配置
5. 降順に並べることを目指す
6. 行き詰まりを検出して戦略を変更

**特徴:**
- より高度なアルゴリズム
- 状態ベースの意思決定
- 動的な戦略変更

## 🛠️ カスタマイズ

### URLの変更

スクリプトの`main()`関数内でURLを変更できます：

```python
async def main() -> None:
    url = 'あなたのゲームURL'  # ここを変更
    player = Game2048AutoPlayer(url)
    await player.play_game(headless=False)
```

### ヘッドレスモードで実行

ブラウザを表示せずに実行する場合：

```python
await player.play_game(headless=True)  # Trueに変更
```

### 待機時間の調整

アニメーション待機時間を調整する場合：

```python
# src/autoplay.py または src/autoplay_advanced.py 内
await asyncio.sleep(0.15)  # この値を変更（秒）
```

## 📁 プロジェクト構成

```
2025-adhoc-blooming-garden-test/
├── src/
│   ├── autoplay.py              # 基本版自動プレイスクリプト
│   └── autoplay_advanced.py     # 高度版自動プレイスクリプト
├── setup.bat                    # セットアップ（バッチファイル版・推奨）
├── setup.ps1                    # セットアップ（PowerShell版）
├── run_autoplay.bat             # 実行（バッチファイル版・推奨）
├── run_autoplay.ps1             # 実行（PowerShell版）
├── pyproject.toml               # プロジェクト設定
├── QUICKSTART.md                # クイックガイド
├── README.md                    # このファイル
└── .gitignore                   # Git除外設定
```

## 📝 ライセンス

MIT License

## 🤝 貢献

プルリクエストを歓迎します！

1. このリポジトリをフォーク
2. フィーチャーブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add some amazing feature'`)
4. ブランチにプッシュ (`git push origin feature/amazing-feature`)
5. プルリクエストを作成

## 🐛 問題報告

バグや問題を見つけた場合は、Issueを作成してください。

## 📚 参考リンク

- [Blooming Garden（ゲーム本体）](https://in0ho1no.github.io/2025-adhoc-blooming-garden/)
- [Playwright Documentation](https://playwright.dev/python/)
- [Original 2048 Game](https://play2048.co/)

## 🎊 謝辞

- ゲーム作成者（Vibe Codingで作成）
- Playwrightの開発チーム
